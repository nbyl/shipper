---
description: Search for relevant infirmation
agent: build
---

Use the tool $TICKET_TOOL to load the ticket with ID $1 as context.

Use the subagent `shippper:research` to collect all relevant information regarding the feature and relevant parts of the used technologies and create a report.

Then save the report back as a comment to the original ticket using the $TICKET_TOOL tool.
